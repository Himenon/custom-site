{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "run-p *:watch",
    "develop": "chmod +x ../lib/cli.js && node ../lib/cli.js -c config.json -Do",
    "develop2": "chmod +x ../lib/cli.js && node ../lib/cli.js --config ./src/config.json -Do",
    "build": "chmod +x ../lib/cli.js && node ../lib/cli.js --config ./config.json --out-dir hoge",
    "build2": "chmod +x ../lib/cli.js && node ../lib/cli.js . --config ./src/config.json",
    "tsc:watch": "tsc -p tsconfig.json -w"
  },
  "dependencies": {
    "@custom-site/custom-site": "file:../"
  },
  "devDependencies": {
    "@types/prismjs": "^1.9.0",
    "npm-run-all": "^4.1.5",
    "prismjs": "^1.15.0",
    "typescript": "^3.2.4"
  }
}
