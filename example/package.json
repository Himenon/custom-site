{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "run-p *:watch",
    "develop": "chmod +x ../lib/cli.js && node ../lib/cli.js -c config.json -Do",
    "develop2": "chmod +x ../lib/cli.js && node ../lib/cli.js --config ./src/config.json -Do",
    "build": "rimraf sample && chmod +x ../lib/cli.js && node ../lib/cli.js --config ./config.json --out-dir sample",
    "build2": "chmod +x ../lib/cli.js && node ../lib/cli.js . --config ./src/config.json",
    "tsc:watch": "tsc -p tsconfig.json -w"
  },
  "dependencies": {
    "@custom-site/custom-site": "file:../"
  },
  "devDependencies": {
    "@types/prismjs": "^1.9.0",
    "@types/url-join": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "pretty": "^2.0.0",
    "prismjs": "^1.15.0",
    "rimraf": "^2.6.3",
    "typescript": "^3.2.4",
    "url-join": "^4.0.0"
  }
}
